function concatenateNames(str1, str2, delimiter){

    console.log(`${str1}${delimiter}${str2}`);
}
concatenateNames('John',
'Smith',
'->');
concatenateNames('Jan',
'White',
'<->');
concatenateNames('Linda',
'Terry',
'=>');